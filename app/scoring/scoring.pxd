# Public declarations are intentionally kept minimal because the extension is
# imported through Python callables in loader.py. This file exists so projects
# embedding the scorer can add typed cimports without changing package layout.
